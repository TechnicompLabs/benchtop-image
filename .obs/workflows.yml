rebuild_on_push:
  steps:
    - trigger_services:
        project: home:technicomp:benchtop:images
        package: tc-benchtop-image
  filters:
    event: push
    branches:
      only:
        - main
